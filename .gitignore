# dependencies
node_modules/
.pnpm-store/

# turbo
.turbo/

# next.js
.next/
out/
*.tsbuildinfo

# wxt / extension build
.output/
.wxt/
dist/

# NOTE: apps/landing-page/public/perfext-extension.zip is intentionally COMMITTED
# so the landing page's "Download latest" button works on static hosts (e.g.
# Vercel), which do not run `pnpm package:extension`. Refresh it with that
# command and commit the result whenever the extension changes.

# env
.env
.env*.local

# misc
.DS_Store
*.log
npm-debug.log*
