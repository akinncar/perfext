# Spec: <init>
Instructions for init project

## Summary
- use turbo repo
- use pnpm
- monorepo with multiple repos landing-page, extension
- use supabase
- not sure if it needs a back-end project for now or if extension can use supabase directly, you can evaluate
- browser extension can use purely JS, or react with querying browser with pure JS.