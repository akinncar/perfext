# Spec: <lp>
Landing page for sell it, not pricing at first version

## Summary
The LP should use next, it should be very similar to cursor landing page (cursor.com/home), it should be clean, show the features, tell about the feature of improving text. the slogan can be `Perfects: make perfect texts`
At this moment no login is needed, you can only show 2 buttons to install the extension: 1 via chrome store, and another by downloading it latest version. it should be very easy to download it.
Page must be clean, and dark theme, simple theme, simple text, and says waht it does and shows download in a few words, sentences, and UI.